-- $$USES "BRC_TOOLS"

if EnviroRead( "BRC_PROJECT_SELECT_IS_DOWN" ) == "TRUE" then
  BRCProject( BRC_BUTTON_PROJECT_PROGRAM, BRC_INPUT_CAMERA_FLOOR );
else
  BRCPreview( BRC_BUTTON_PREVIEW_FLOOR, BRC_BUTTON_XK24_PREVIEW_KEYS, BRC_INPUT_CAMERA_FLOOR, EnviroRead( "BRC_AUTO_ON" ) == "TRUE" and "cut" );
end;
