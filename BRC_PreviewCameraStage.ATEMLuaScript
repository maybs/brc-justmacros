-- $$USES "BRC_TOOLS"

if EnviroRead( "BRC_PROJECT_SELECT_IS_DOWN" ) == "TRUE" then
  BRCProject( BRC_BUTTON_PROJECT_PROGRAM, BRC_INPUT_CAMERA_STAGE );
else
  BRCPreview( BRC_BUTTON_PREVIEW_STAGE, BRC_BUTTON_XK24_PREVIEW_DRUMS, BRC_INPUT_CAMERA_STAGE, EnviroRead( "BRC_AUTO_ON" ) == "TRUE" and "cut" );
end;
